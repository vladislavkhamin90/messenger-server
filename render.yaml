services:
  - type: web
    name: notes-backend
    env: kotlin
    buildCommand: ./gradlew build
    startCommand: ./gradlew run
    envVars:
      - key: PORT
        value: 8080
      - key: JAVA_OPTS
        value: -Xmx256m -Xss512k -XX:+UseCompressedOops
